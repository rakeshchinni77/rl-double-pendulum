# Training Configuration
TIMESTEPS=500000
REWARD_TYPE=shaped
MODEL_PATH=models/ppo_model.zip

# Logging
LOG_DIR=logs/

# Optional evaluation defaults
EPISODES=1
MAX_STEPS=300
GIF_PATH=media/agent_final.gif
FPS=30

# Docker/headless rendering (optional)
SDL_VIDEODRIVER=dummy
DISPLAY=